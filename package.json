{
  "name": "cheatle-api",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "prepare:dictionary": "ts-node src/scripts/extractCheatleDictionary.ts static/dictionary.txt",
    "build": "tsc",
    "setup": "npm run prepare:dictionary && npm run build",
    "start": "cross-env NODE_ENV=production node dist/server.js",
    "dev": "cross-env NODE_ENV=development ts-node-dev src/server.ts"
  },
  "author": "smyler3",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-cron": "^4.2.1",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.5",
    "@types/node": "^24.9.1",
    "cross-env": "^10.1.0",
    "typescript": "^5.9.3"
  }
}
